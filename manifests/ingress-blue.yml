apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: my-app-blue
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: example.com
    http:
      paths:
      - path: /blue
        pathType: Prefix
        backend:
          service:
            name: my-app-blue
            port:
              name: http
